<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobdeve.s17.lim.matthew.mobdeve_progplan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobdeveprogplan">
        <activity android:name=".ViewIndivProgActivity"/>
        <activity android:name=".UpdateProgActivity" />
        <activity android:name=".EditProgActivity" />
        <activity android:name=".ViewProgsActivity" />
        <activity android:name=".EvaluateProgActivity" />
        <activity android:name=".CreateProgActivity" />
        <activity android:name=".UserRegActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
